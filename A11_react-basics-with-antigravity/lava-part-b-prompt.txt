Analyze `@src\App.css` and `@src\components\tracks\SongList.css` and overhaul their existing styles into a cute, retro 8-bit handheld game aesthetic using a soft pastel color palette featuring pastel pink, mint, lavender, soft yellow, and cream. Refactor `@src\App.css` by upgrading the `body`, `.title`, and `.page` rules: replace the `script` font family on `.title` with a clean pixel-style or monospace font, refine the `#ffb6e8` background with a soft pixelated frame or grid design, and format `.page` into a compact, boxy mobile container that feels like a cute handheld console. Upgrade `@src\components\tracks\SongList.css` by removing the smooth `border-radius: 8px` on `.song-card` and replacing it with sharp 0px corners, thick solid dark-pastel borders, hard offset pixel drop shadows like `box-shadow: 4px 4px 0px #3a2e39`, and tactile hover/active states like `transform: translate(2px, 2px)` to mimic pressable retro buttons. Do not create any new files or add external libraries; apply all new styling and visual improvements directly inside the existing `@src\App.css` and `@src\components\tracks\SongList.css` files.